<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.team.sportsskuyy">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application

        android:allowBackup="true"
        android:icon="@mipmap/icons"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icons"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Dokumentasi"></activity>
        <activity android:name=".ExpertStepper"
            android:screenOrientation="portrait"/>
        <activity android:name=".MediumStepper"
            android:screenOrientation="portrait"/>
        <activity android:name=".EasyStepper"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".Kalori"
            android:label="@string/title_activity_kalori" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>